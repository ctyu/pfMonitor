require('QClass');
var QClass = window.QClass,
    ns = window.pfMonitor.common,
    probeNames = {
        'firstPaint' : true,
        'firstFrame' : true,
        'netWorkType' : true,
        'jsError' : true
    }

QClass.define('pfMonitor.Probes.DefaultH5Probe',{
    'extend' : ns.AbstractProbe,

    'initialize' : function(opts){
        this.
    },

    'process' : function(){

    }
})